"""CL-bench v1 benchmark modules.

V1 is the original implementation using LangGraph for ACE inference
and simple prefix-slice sampling.

Modules
-------
infer_baseline  – GPT-5.1 baseline inference.
infer_ace       – ACE inference via LangGraph pipeline.
"""
